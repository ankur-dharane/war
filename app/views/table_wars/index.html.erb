<h1>Listing table_wars</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @table_wars.each do |table_war| %>
  <tr>
    <td><%= link_to 'Show', table_war %></td>
    <td><%= link_to 'Edit', edit_table_war_path(table_war) %></td>
    <td><%= link_to 'Destroy', table_war, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Table war', new_table_war_path %>
